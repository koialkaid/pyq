# Koi 博客新电脑接管指南

最后更新：2026-07-26（Asia/Shanghai）

本指南不包含密码、Token、数据库连接串或存储密钥。生产环境秘密保存在 Vercel，内容保存在 TiDB 和 Supabase，不应提交到 GitHub。

## 一、只在线写文章

只写文章、管理媒体或修改站点设置时，新电脑不需要安装 Git、Node.js，也不需要旧电脑开机。

1. 打开管理后台：<https://koialkaid-blog.vercel.app/admin/login>
2. 使用博客管理员邮箱或用户名和管理员密码登录。
3. 在后台创建文章、动态，或管理媒体和设置。

公开地址：

- 博客：<https://koialkaid-blog.vercel.app>
- 后端健康检查：<https://koialkaid-blog-api.vercel.app/api/health>
- GitHub：<https://github.com/koialkaid/pyq>
- 旧博客：<https://koialkaid.github.io/blog/>

## 二、首次克隆并检查代码

以下命令适用于 Windows PowerShell。先安装 Git 和 Node.js LTS；如果已经安装，可以跳过对应的 `winget` 命令。

```powershell
winget install --id Git.Git -e
winget install --id OpenJS.NodeJS.LTS -e
```

安装完成后重新打开 PowerShell，然后原样执行：

```powershell
$workRoot = Join-Path $HOME "Projects"
New-Item -ItemType Directory -Force -Path $workRoot | Out-Null
Set-Location $workRoot

git clone https://github.com/koialkaid/pyq.git
Set-Location .\pyq
git checkout master
git pull --ff-only origin master

Set-Location .\backend
npm.cmd ci
npm.cmd run build

Set-Location ..\frontend
npm.cmd ci
npm.cmd run build

Set-Location ..
git status --short --branch
```

正常结果应包括：

- 当前分支为 `master`。
- `master` 与 `origin/master` 同步。
- 前后端构建成功。
- `git status` 没有未预期修改。

## 三、日常代码更新

每次开始工作前：

```powershell
Set-Location "$HOME\Projects\pyq"
git checkout master
git pull --ff-only origin master
git status --short --branch
```

修改代码后先构建：

```powershell
Set-Location "$HOME\Projects\pyq\backend"
npm.cmd run build

Set-Location "$HOME\Projects\pyq\frontend"
npm.cmd run build
```

确认无误后提交：

```powershell
Set-Location "$HOME\Projects\pyq"
git status --short
git add -- <本次修改的文件>
git commit -m "说明本次修改"
git push origin master
```

`git push origin master` 后，Vercel 会自动触发两个生产部署：

- `koialkaid-blog-api`，Root Directory 为 `backend`。
- `koialkaid-blog`，Root Directory 为 `frontend`。

在 Vercel 中确认两个部署均为 `Ready`，再检查首页、后台登录页和健康接口。

## 四、本地开发

前端本地开发：

```powershell
Set-Location "$HOME\Projects\pyq\frontend"
npm.cmd run dev
```

生产服务依赖 Vercel 中的环境变量，本地仅构建前后端不需要生产密钥。

只有在新电脑本地运行后端、访问 TiDB、执行存储测试、数据库迁移或备份时，才需要重新建立：

```text
backend/.env
```

该文件不能从 GitHub 获得，这是有意的安全设计。所需秘密应从密码管理器或对应服务控制台恢复：

- TiDB Cloud：数据库主机、端口、用户名、密码和数据库名。
- Supabase：项目 URL、服务端 Secret Key、Bucket 和公开媒体地址。
- 博客：管理员配置及内部随机密钥。
- Vercel：以当前生产项目的环境变量为准。

不要通过聊天、Issue、截图、提交或公开云盘传递 `.env`。

建立 `.env` 后，本地启动后端：

```powershell
Set-Location "$HOME\Projects\pyq\backend"
npm.cmd run dev
```

## 五、Vercel CLI（可选）

正常更新通过 GitHub 自动部署，不需要 Vercel CLI。只有排查部署或紧急手工部署时才安装：

```powershell
npm.cmd install --global vercel
vercel login
```

项目名称和团队：

- Team：`koi-alkaid-projects`
- 前端：`koialkaid-blog`
- 后端：`koialkaid-blog-api`

## 六、备份

当前仓库提供数据库 JSON 备份命令，但执行它需要有效的 `backend/.env`：

```powershell
Set-Location "$HOME\Projects\pyq\backend"
npm.cmd run db:backup
```

备份写入 `backend/backups/`。该目录被 Git 忽略，因为备份包含完整数据库数据。应将备份加密保存到独立的私有存储，不能提交 GitHub。

生产数据位置：

- GitHub：应用源代码和项目文档。
- Vercel：前后端运行环境和生产环境变量。
- TiDB Cloud：文章、动态、账号、评论、设置和媒体元数据。
- Supabase Storage：图片、PDF 和其他媒体文件。

## 七、不得误操作的资源

- 不要删除或覆盖旧仓库 `https://github.com/koialkaid/blog`。
- 不要关闭旧站 `https://koialkaid.github.io/blog/`，它作为归档和友情链接保留。
- 不要把 `backend/.env`、数据库备份、Token 或密钥加入 Git。
- 不要对 TiDB 执行 `force`、破坏性重建或无备份的数据清理。
- 不要删除 Supabase 文件，除非已经确认没有文章、媒体库或个人资料引用。

## 八、交给新电脑 Codex 的完整指令

以下内容可以原样发送给新电脑上的 Codex，不需要再补充仓库地址、部署地址或项目背景：

```text
请接管并继续维护我的 Koi 云博客项目。

代码仓库：https://github.com/koialkaid/pyq.git
默认分支：master
建议本地目录：$HOME\Projects\pyq
公开博客：https://koialkaid-blog.vercel.app
管理后台：https://koialkaid-blog.vercel.app/admin/login
后端健康检查：https://koialkaid-blog-api.vercel.app/api/health
旧博客归档：https://koialkaid.github.io/blog/

如果本地仓库不存在，请执行：
git clone https://github.com/koialkaid/pyq.git $HOME\Projects\pyq

然后：
1. 进入仓库并检出 master，执行 git pull --ff-only origin master。
2. 首先完整阅读 项目日志.md、新电脑接管指南.md、部署文档.md 和 backend/VERCEL_DEPLOYMENT.md。
3. 检查 git status，不要覆盖或回退现有未提交修改。
4. 在 backend 和 frontend 分别执行 npm.cmd ci 与 npm.cmd run build。
5. 检查三个线上地址是否正常；生产数据位于 TiDB，媒体位于 Supabase。
6. 所有代码修改完成后运行对应构建并做针对性测试，再提交到 master；git push 会自动触发前后端 Vercel 部署。
7. 不要要求我重新说明仓库地址、服务分工和旧站关系，这些内容以仓库文档为准。
8. 不要读取、打印、提交或在聊天中索要密码、Token、数据库连接串和 Supabase Secret Key。普通代码构建不需要本地秘密；只有本地运行后端或执行数据库/存储维护时才需要由我在新电脑安全配置 backend/.env。
9. 不要修改或删除 koialkaid/blog 旧仓库和旧 GitHub Pages 地址。
10. 当前已知待办：Trellis 文章有 4 张失效的飞书外链图片，找到原图后需上传 Supabase 并在后台替换；Cloudflare R2、自定义域名和云端定期备份属于后续可选工作。

先汇报仓库状态、构建结果和线上健康状态，然后继续处理我在新电脑提出的具体任务。
```

## 九、更多项目记录

完整历史、迁移记录、R2 切换步骤和安全规则见：

- `项目日志.md`
- `部署文档.md`
- `backend/VERCEL_DEPLOYMENT.md`

