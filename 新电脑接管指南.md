# Koi 博客新电脑接管指南

最后更新：2026-08-06（Asia/Shanghai）

本指南面向一台没有旧聊天上下文、没有项目文件的全新 Windows 电脑。文档不包含密码、Token、数据库连接串或存储密钥。

## 一、先确认：换电脑不会影响线上博客

正式博客完全运行在云端，旧电脑可以关机：

- 代码和文档：GitHub `koialkaid/pyq`
- 前端：Vercel `koialkaid-blog`
- 后端：Vercel `koialkaid-blog-api`
- 文章、账号、评论和设置：TiDB Cloud
- 图片、PDF 和其他媒体：Supabase Storage
- 旧博客归档：GitHub Pages，必须继续保留

公开地址：

- 博客：<https://koialkaid-blog.vercel.app>
- 管理后台：<https://koialkaid-blog.vercel.app/admin/login>
- 文章目录：<https://koialkaid-blog.vercel.app/articles>
- 后端健康检查：<https://koialkaid-blog-api.vercel.app/api/health>
- GitHub：<https://github.com/koialkaid/pyq>
- 旧博客：<https://koialkaid.github.io/blog/>

只写文章、管理媒体或修改站点设置时，直接使用线上管理后台，不需要安装开发环境，也不需要 `backend/.env`。

## 二、离开旧电脑前必须带走什么

代码不需要手工复制，以 GitHub `master` 为准。真正不在 GitHub 的本地文件只有：

1. `backend/.env`：本地数据库、存储和管理员维护配置。
2. `backend/backups/` 中最新的 TiDB JSON 备份。

安全转移方法：

- 优先使用加密 U 盘、密码管理器的安全附件或端到端加密存储。
- 不要通过聊天、邮件、Issue、截图或公开网盘传递 `.env` 和备份。
- 不要把整个项目连同 `.env` 压缩后上传普通云盘。
- 新电脑接管完成后，确认旧电脑磁盘的处理方式；如果旧电脑不再由本人控制，应安全清除本地秘密。

不需要复制：

- `node_modules/`、`.next/`、`dist/` 等构建产物。
- `E:\CPA\pyq` 整个目录，因为代码可以重新克隆。
- `blog-old` 整个目录，因为旧博客也在 GitHub。
- Vercel 环境变量；它们已经保存在 Vercel 项目中。

## 三、全新电脑首次安装

在 Windows PowerShell 中执行：

```powershell
winget install --id Git.Git -e
winget install --id OpenJS.NodeJS.LTS -e
```

安装完成后重新打开 PowerShell，检查：

```powershell
git --version
node --version
npm.cmd --version
```

克隆项目：

```powershell
$workRoot = Join-Path $HOME "Projects"
New-Item -ItemType Directory -Force -Path $workRoot | Out-Null
Set-Location $workRoot

git clone https://github.com/koialkaid/pyq.git
Set-Location .\pyq
git checkout master
git pull --ff-only origin master
git status --short --branch
```

安装依赖并构建：

```powershell
Set-Location "$HOME\Projects\pyq\backend"
npm.cmd ci
npm.cmd run build

Set-Location "$HOME\Projects\pyq\frontend"
npm.cmd ci
npm.cmd run build

Set-Location "$HOME\Projects\pyq"
git status --short --branch
```

正常结果：当前分支是 `master`、与 `origin/master` 同步、前后端构建成功、没有未预期修改。

## 四、恢复本地秘密和备份（仅维护数据库时需要）

普通代码开发和构建不需要生产密钥。只有以下操作需要 `backend/.env`：

- 本地启动后端。
- 直接访问 TiDB。
- 执行数据库迁移、初始化或备份。
- 执行 Supabase 存储测试。
- 执行旧博客迁移脚本。

需要时，将安全转移的文件放回：

```text
$HOME\Projects\pyq\backend\.env
$HOME\Projects\pyq\backend\backups\<备份文件>.json
```

然后确认它们仍被 Git 忽略：

```powershell
Set-Location "$HOME\Projects\pyq"
git check-ignore -v backend/.env backend/backups/*
git status --short
```

严禁运行 `git add -f backend/.env`，严禁提交数据库备份。

当前生产数据库已经完成文章合集字段迁移。新电脑首次克隆后不要因为看到迁移脚本就重复执行 `db:init`；只有后续版本明确要求数据库迁移、已经完成最新备份且理解影响范围时才执行。

## 五、接管后的线上检查

在浏览器打开：

1. <https://koialkaid-blog.vercel.app>
2. <https://koialkaid-blog.vercel.app/articles>
3. <https://koialkaid-blog.vercel.app/admin/login>
4. <https://koialkaid-blog-api.vercel.app/api/health>

正常情况下前三个页面可以打开，健康接口返回包含 `"status":"ok"` 的 JSON。

GitHub `master` 推送会自动触发两个 Vercel Production 部署：

- 前端项目 `koialkaid-blog`，Root Directory 为 `frontend`。
- 后端项目 `koialkaid-blog-api`，Root Directory 为 `backend`。

## 六、当前代码状态和下一项任务

功能基线提交：`7d56c07`（远端后续交接文档提交也属于正常更新，应始终以最新 `origin/master` 为准）。

已完成：

- 公开文章目录及全部/分类/合集视图。
- 后台分类、合集、合集篇序设置。
- 已有分类/合集的可搜索选择。
- 合集篇序自动加一、插入顺延和同合集调换。
- 桌面章节/合集分段目录、移动端折叠合集目录。
- 分类/合集 URL 筛选和分页。

当前明确未修复问题：

- 文章详情左侧“章节/合集”存在异步竞态：合集请求可能先于章节提取完成，导致页面短暂卡顿后默认切到合集。
- 正确要求：只要文章有章节，默认永远显示章节；合集异步加载不得改变默认选项。只有章节提取完成且确认没有章节时，才允许自动显示合集。
- 修复时还应移除章节数量变化导致的重复合集请求，并保持用户主动选择的目录模式。

低优先级内容缺口：Trellis 文章有 4 张已失效的飞书外链图片，只有找到原图后才能替换，不影响当前接管。

## 七、日常开发与发布

开始工作前：

```powershell
Set-Location "$HOME\Projects\pyq"
git checkout master
git pull --ff-only origin master
git status --short --branch
```

修改后：

```powershell
Set-Location "$HOME\Projects\pyq\backend"
npm.cmd run build

Set-Location "$HOME\Projects\pyq\frontend"
npm.cmd run build

Set-Location "$HOME\Projects\pyq"
git diff --check
git status --short
```

只添加本次文件，提交并推送：

```powershell
git add -- <本次文件>
git commit -m "准确说明本次修改"
git push origin master
```

推送后检查两个 Vercel 部署和四个线上地址。

## 八、安全边界

- 不读取、打印、提交或在聊天中发送 `.env` 内容、密码、Token、Secret Key 和连接串。
- 不使用 `git reset --hard`，不回退不属于当前任务的修改。
- 不执行 `npm audit fix --force`。
- 不对 TiDB 使用 `force`、破坏性重建或无备份清理。
- 不删除仍被正文、头像、封面或媒体库引用的 Supabase 对象。
- 不删除旧博客仓库或关闭旧 GitHub Pages 地址。
- 修改生产数据库前先生成最新备份并确认影响范围。

## 九、交给新电脑 GPT/Codex

仓库根目录提供独立文件：`新电脑GPT启动指令.txt`。在全新电脑安装 Codex 后，可以直接把该文件全文作为第一条消息发送。

完整历史和部署资料：

- `项目日志.md`
- `部署文档.md`
- `backend/VERCEL_DEPLOYMENT.md`
- `新电脑GPT启动指令.txt`
