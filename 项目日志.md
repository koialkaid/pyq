# KoiAlkaid 博客项目日志

最后更新：2026-08-06（Asia/Shanghai）

本文记录部署进度、架构决策、恢复方法，以及未来从 Supabase Storage 切换到 Cloudflare R2 的步骤。

> 安全要求：本文禁止记录密码、Token、Access Key、完整数据库连接串、付款信息和其他秘密。

## 协作约定

- Codex 每次回复（包括工作过程更新和最终答复）都必须在开头写明：`预计需要xx时xx分钟完成。`
- 预计时间应覆盖当次回复所涉及的阅读、开发、验证和汇报工作；如果工作范围变化，应在后续更新中给出新的预计时间。
- 用户尚未提出具体新需求时，Codex 应等待，不得自行开展开发、部署或生产数据操作。

## 待开发需求

### 当前第一优先级：章节/合集默认状态竞态

- 文章详情加载时可能先显示章节目录，随后卡顿并错误地默认切换到合集目录。
- 原因是合集请求和正文标题提取并行执行；合集请求先完成时，代码把“章节尚未提取”误判成“文章没有章节”。
- 正确行为：只要文章存在章节标题，默认永远显示章节目录；合集数据异步加载不得改变默认选项。
- 只有章节提取已经完成并确认没有章节、同时文章确实属于合集时，才自动显示合集目录。
- 修复时应增加明确的章节提取完成状态、避免章节数量变化触发重复合集请求，并保留用户主动选择的目录模式。

### 文章分类与合集编辑体验

- 文章编辑器中的“分类”和“合集”应支持从已有值中下拉选择，同时保留输入新值的能力，使用可搜索的组合选择框。
- 选择已有合集后，合集篇序默认自动填写为该合集当前最大篇序 `+1`；用户仍可手动指定篇序。
- 手动指定的篇序与现有文章冲突时，采用“插入并顺延”：新文章占用指定篇序，原该位置及其后的文章统一顺延一位。
- 编辑已有合集文章时，允许用户在“插入并顺延”和“与目标篇调换”两种模式中选择；默认使用插入模式。
- 调换模式只适用于同一合集内两篇已有文章。新文章或从其他合集移入的文章没有可交换的原位置，只能追加或插入。
- 插入模式应先从原位置移出，再插入目标位置并整理连续篇序；调换模式只交换两篇文章的篇序。两种操作都必须保证同一合集内篇序唯一。
- 后台保存前应预览结果，例如“将插入为第 3 篇，后续 4 篇顺延”或“将与《文章标题》交换篇序”。

### 文章详情侧栏导航

- 桌面端现有左侧导航改为单一卡片内的“章节目录 / 合集目录”分段切换，不上下堆叠两个长目录，也不新增更靠左的第四列。
- 两个目录共用卡片高度和独立滚动区域，切换控件固定在卡片顶部，避免章节较长时把合集目录挤出首屏。
- 合集目录显示完整篇目顺序、当前文章高亮和篇序，点击可直接切换到合集内其他文章；当前篇目应自动滚动到可见位置。
- 文章没有合集时不显示合集切换项；没有可用章节但存在合集时，默认直接显示合集目录。
- 用户主动选择合集目录后，在同一合集内连续跳转文章时应保持合集目录选中，不要每篇文章都重置回章节目录。
- 分类属于宽泛主题，不作为第三个长目录，避免与章节、合集争夺空间；显示轻量分类入口、同分类文章数量和“查看全部”即可。
- 文章顶部现有分类标签应改为可点击，进入公开文章目录中对应分类的筛选结果。
- 移动端不显示桌面侧栏：合集目录应在正文顶部或当前底部合集导航中提供可折叠篇目列表，分类继续使用可点击标签。
- 章节目录、合集目录和分类入口均沿用现有卡片宽度、间距、圆角、颜色变量和激活态，不引入新的视觉参数。

## 一、项目目标

- 将 `zhjurz/pyq` 部署为新的在线博客，并使用其自带管理后台。
- 保留旧 GitHub Pages 博客，并在新博客中将旧地址添加为友情链接。
- 迁移旧博客文章、散记、日期、元数据和媒体文件。
- 正式环境完全运行在云端，不依赖当前电脑开机。
- 暂时使用平台免费域名，以后再绑定自有域名。
- 暂时使用无需银行卡的媒体存储，以后可迁移到 Cloudflare R2。

## 二、仓库和本地路径

- 上游仓库：`https://github.com/zhjurz/pyq`
- 用户 Fork：`https://github.com/koialkaid/pyq`
- 旧博客仓库：`https://github.com/koialkaid/blog`
- 旧博客地址：`https://koialkaid.github.io/blog/`
- 当前本地仓库：`E:\CPA\pyq`
- 本地后端配置：`E:\CPA\pyq\backend\.env`（已被 Git 忽略，严禁提交）

旧博客仓库和 GitHub Pages 必须保持不变。新博客使用独立仓库和独立部署。

## 三、当前架构

| 功能 | 服务 | 状态 |
| --- | --- | --- |
| 源代码 | GitHub `koialkaid/pyq` | 已完成 Fork |
| 前端运行环境 | Vercel `koialkaid-blog` | 已上线 |
| 后端 API | Vercel `koialkaid-blog-api` | 已上线 |
| 文章、账号、评论和设置 | TiDB Cloud Starter | 已初始化 |
| 临时媒体存储 | Supabase Storage | 已配置并通过API测试 |
| 未来媒体存储 | Cloudflare R2 | 等具备付款方式后启用 |

### TiDB Cloud 状态

- 实例名称：`koialkaid-blog`
- 套餐：Starter
- 每月消费上限：`$0`
- 区域：AWS Tokyo（`ap-northeast-1`）
- 分支：`main`
- 应用数据库：`moment_blog`
- TLS：必须开启
- 数据库表初始化：成功
- 初始管理员：创建成功

TiDB 主机、用户名和密码不得写入本文。生产值只存放在 Vercel 环境变量中；本地维护时可放入 Git 已忽略的 `backend/.env`。

## 四、工作日志

### 2026-08-06（全新电脑迁移交接）

- 重写 `新电脑接管指南.md`，覆盖全新 Windows 电脑安装、克隆、构建、本地秘密恢复、线上检查和安全边界。
- 新增 `新电脑GPT启动指令.txt`，可在新电脑直接作为 GPT/Codex 第一条接管消息使用。
- 记录当前第一优先级问题：章节/合集目录的异步竞态会造成加载闪动和错误默认合集，尚未修复。
- 生成离开旧电脑前的最新 TiDB 备份 `tidb-2026-08-06T10-20-24-860Z.json`（15 表、61 行）。该文件位于 Git 忽略目录，必须通过加密介质转移，不得提交或公开分享。
- 代码和非敏感文档以 GitHub `master` 为准；新电脑无需复制 `node_modules`、构建产物或整个旧工作目录。

### 2026-08-06（文章整理与目录导航完善）

- 增加文章整理元数据接口，后台可读取已有分类、合集、合集篇目和最大篇序。
- 文章保存支持事务化的合集排序：新文章/跨合集文章使用插入并顺延；同一合集已有文章可选择插入或调换；删除和移出合集后自动压紧篇序。
- 后台分类和合集改为可搜索的组合选择输入，选择已有合集时自动填充下一个篇序，并在冲突时展示调整方式和结果提示。
- 桌面文章详情左侧导航改为“章节/合集”分段切换，合集目录自动高亮当前文章并滚动到可见位置。
- 移动端合集阅读块增加可折叠的完整篇目列表，分类标签可跳转到分类筛选页。
- 公开 `/articles` 支持 `category`、`series` 和 `page` URL 参数，筛选和分页链接可在刷新、分享和返回时保留。
- 前后端构建通过，本地文章目录、分类筛选和合集筛选均返回 HTTP 200。本轮未新增数据库字段，不需要再次运行 `db:init`。

### 2026-08-05（文章目录、分类与合集）

- 新增公开文章目录页 `/articles`，沿用现有特殊页面布局与站点视觉变量，支持“全部、分类、合集”三种浏览方式。
- 桌面侧栏“文章列表”标题改为可点击入口；移动端可直接访问公开文章目录地址。
- 恢复后台文章分类设置，并增加合集名称与合集篇序字段。
- 文章详情页会显示合集阅读进度，以及合集内上一篇、下一篇导航。
- 增加可重复执行的增量迁移 `migrate-article-series.ts`，并接入 `npm.cmd run db:init`。
- 修复数据库备份脚本，使代码模型先于数据库新增字段时仍能按数据库实际列生成迁移前快照。
- 已生成迁移前备份 `tidb-2026-08-05T10-09-05-125Z.json`（15 表、60 行），备份位于 Git 忽略目录，禁止提交或公开分享。
- 已对生产 TiDB 受控执行 `npm.cmd run db:init`，成功新增 `posts.series` 和 `posts.series_order`；已有管理员、站点设置和业务内容均未被重置。
- 前后端生产构建通过，随后推送 `master` 触发 Vercel 正式部署。

### 2026-07-26

- 检查旧 GitHub Pages 博客，确认原写作后台为 Pages CMS。
- 确定使用 `zhjurz/pyq` 作为新博客。
- 将项目 Fork 到 `koialkaid/pyq`，默认分支为 `master`。
- 将 Fork 克隆到 `E:\CPA\pyq`。
- 在 AWS Tokyo 创建免费的 TiDB Cloud Starter 实例。
- 将 TiDB 每月消费上限设置为 `$0`。
- 创建 `moment_blog` 数据库，字符集为 `utf8mb4`，排序规则为 `utf8mb4_unicode_ci`。
- 首次 TiDB 密码因出现在聊天中而立即作废，已完成密码轮换。只有轮换后的密码有效。
- 使用 `npm.cmd ci` 安装后端依赖。
- 创建 Git 已忽略的 `backend/.env`。
- 使用 `npm.cmd run db:init` 完成数据库初始化。
- 已创建应用表、默认站点设置、默认歌单、管理员和 About 页面。
- 评估 Cloudflare R2：虽有免费额度，但开通时需要付款方式。
- 因当前无法绑定银行卡，暂缓开通 R2。
- 决定临时采用 Supabase Storage；需先实现并测试存储适配层。
- 创建 Supabase Free项目 `koialkaid-blog-media`，区域为 Northeast Asia（Tokyo）。
- 创建公开 Bucket `koialkaid-media`，单文件限制为50MB。
- 增加可通过 `STORAGE_PROVIDER` 切换的 Supabase/R2统一存储层。
- 完成签名上传、对象校验、移动、公开读取和删除测试。
- 增加 `npm.cmd run storage:smoke`维护命令，供未来切换R2后复用。
- 修复传统后端入口未将 `PORT`环境变量显式转换为数字的问题。

### 2026-07-26（Vercel 正式部署）

- 创建 Vercel 后端项目 `koialkaid-blog-api`，并将生产环境变量以 Sensitive 形式保存。
- 后端正式地址：`https://koialkaid-blog-api.vercel.app`。
- 后端 `/api/health` 已通过线上检查，并能正常连接 TiDB。
- 创建 Vercel 前端项目 `koialkaid-blog`，配置 API 转发、站点地址、Supabase 媒体域名和重验证密钥。
- 修复 Vercel 将前端误识别为 `Other` 的问题，框架预设已改为 `Next.js`。
- 关闭前端项目自动启用的 Vercel SSO Deployment Protection，使公开博客无需 Vercel 团队账号即可访问。博客管理权限仍由应用自己的管理员登录保护。
- 前端正式地址：`https://koialkaid-blog.vercel.app`。
- 首页、`/admin/login` 和前端 `/api/health` 转发均返回 HTTP 200。
- 使用本地保存的管理员凭据验证线上登录成功；凭据和登录 Token 未写入日志或 Git。
- 完成生产环境 Supabase 存储闭环测试：签名、上传、TiDB 确认、公开读取、删除均成功，测试文件和记录已清理。
- 修复 `/admin/login` 被后台鉴权布局误判为未登录页面并重定向到首页的问题；登录页现已从后台侧栏和登录令牌检查中排除。
- 首次部署由 Vercel CLI 完成；两个项目的 Root Directory 已分别设置为 `backend` 和 `frontend`。
- Vercel GitHub App 已按最小权限仅授权 `koialkaid/pyq`；前后端两个项目均已连接该仓库，生产分支为 `master`。

### 2026-07-26（旧博客迁移）

- 只读克隆旧仓库到 `E:\CPA\blog-old`，旧仓库和 GitHub Pages 均未修改。
- 增加 `npm.cmd run blog:migrate-legacy -- --source=<旧仓库路径>` 预览命令；追加 `--apply` 执行幂等迁移。
- 使用 TOML 和 Markdown 正式解析器处理 Front Matter、正文、表格、代码块、脚注、图片和 Hugo PDF shortcode。
- 迁移 6 篇旧文章、1 条旧随记和 1 个归档 About 页面；全部保留原始发布日期、摘要及可表达的分类/系列/标签。
- 旧随记“星露谷”迁移为动态，其余内容迁移为文章；每项通过旧站原文 URL 去重，重复执行只更新、不重复创建。
- 上传旧站头像、2 个正文图片资源和 2 份 PDF 到 Supabase，共建立 5 条媒体记录；5 个对象公网 HEAD 检查均返回 HTTP 200。
- Trellis 文章中的 4 张飞书外链图片无法复制：源接口返回 `application/json` 而不是图片。正文保留原链接，后续取得原图后需在后台替换。
- 添加旧站友情链接 `https://koialkaid.github.io/blog/`，名称为 `Koi's Blog（旧站）`。
- 验证两篇代表性文章页面返回 HTTP 200，Markdown HTML、Supabase 图片和 2 个 PDF 链接均存在。
- 完成线上草稿创建、修改和删除回归测试，测试草稿已清理。
- 增加 `npm.cmd run db:backup` 私有 JSON 备份命令；备份目录 `backend/backups/` 由 Git 忽略。
- 已生成迁移前快照（15 表、8 行）和迁移后快照（15 表、22 行）。备份包含完整数据库数据，禁止上传或公开分享。

## 五、安全规则

- 不得在聊天、截图、Git提交、Issue或文档中发送密码、Token和连接串。
- 秘密一旦出现在聊天或截图中，必须立即轮换。
- `backend/.env` 必须保持 Git 忽略状态。
- TiDB、博客管理员、GitHub、Vercel、Cloudflare和 Supabase使用不同密码。
- 所有平台开启两步验证。
- 存储密钥尽量限制为只能访问博客的单个 Bucket。
- Vercel秘密通过 Project Settings配置，不写入仓库。
- 分享截图前遮挡密码、Token、账号ID、个人邮箱和IP地址。

## 六、剩余工作

- [x] Fork 上游仓库。
- [x] 克隆用户 Fork。
- [x] 创建免费 TiDB Starter实例。
- [x] 创建 `moment_blog` 数据库。
- [x] 初始化数据库和首个管理员。
- [x] 注册或登录 Supabase。
- [x] 创建博客专用媒体 Bucket。
- [x] 在后端增加与提供商无关的存储适配层。
- [x] 实现 Supabase签名上传和公开媒体地址。
- [x] 保留原有 R2实现，使存储提供商可切换。
- [x] 通过API验证登录、签名、上传、确认、公开读取和删除。
- [x] 测试图片上传、删除、媒体库、文章封面和富文本嵌入。
- [x] 创建 Vercel 后端项目；当前由 `backend` 目录通过 CLI 部署。
- [x] 配置后端生产环境变量。
- [x] 部署并验证后端健康接口。
- [x] 创建 Vercel 前端项目；当前由 `frontend` 目录通过 CLI 部署。
- [x] 配置前端生产环境变量。
- [x] 部署并验证公开页面及 `/admin` 登录。
- [x] 为 Vercel GitHub App 授权 `koialkaid/pyq`，连接两个项目并配置各自 Root Directory。
- [x] 迁移旧博客 Markdown内容和可取得的媒体。
- [x] 尽可能保留原发布日期、摘要、分类、系列和标签。
- [x] 将旧博客地址添加为友情链接。
- [ ] 检查桌面和手机页面。
- [x] 迁移前后各导出一份 TiDB 数据快照。
- [x] 建立手动数据库备份命令并保持备份文件不进入 Git。
- [ ] 建立数据库和媒体的云端定期备份流程。

## 七、临时 Supabase Storage 方案

Supabase只用于保存媒体，文章数据库继续使用 TiDB。

实现要求：

- 增加 `STORAGE_PROVIDER=supabase` 或 `STORAGE_PROVIDER=r2`。
- 所有提供商秘密只保存在后端。
- Supabase `service_role`密钥不得发送到浏览器。
- 使用签名直传，避免大文件经过 Vercel Serverless请求体。
- 使用不依赖提供商的对象路径，例如：
  - `articles/YYYY/MM/<id>.<ext>`
  - `avatars/<id>.<ext>`
  - `media/<id>.<ext>`
- 保存迁移所需元数据：对象路径、MIME类型、大小、当前提供商，以及条件允许时的校验值。
- 免费额度主要用于图片；在启用 R2前避免上传大量视频和音频。

## 八、以后从 Supabase 迁移到 R2

迁移验证全部通过之前，不得删除 Supabase中的源文件。

### 1. 准备 R2

1. 有可用付款方式后开通 Cloudflare R2。
2. 创建一个 Standard存储桶，建议命名为 `koialkaid-media`。
3. 开启用量或账单提醒。
4. 创建仅限该 Bucket的 Object Read & Write Token。
5. 配置稳定的公开媒体地址；初期可用 `r2.dev`，以后改用 `media.<自有域名>`。
6. 为正式 Vercel前端地址配置 R2 CORS。

### 2. 复制和校验

1. 迁移前导出 TiDB备份。
2. 导出 Supabase对象及元数据清单。
3. 保持对象路径不变，将文件逐个复制到 R2。
4. 对比对象数量、文件大小、MIME类型和可用的校验值。
5. 生成迁移报告，记录成功、跳过和失败项目。
6. 重试失败项目，直到源端和目标端清单一致。

### 3. 切换应用

1. 将 R2凭据添加到 Vercel后端环境变量。
2. 设置 `STORAGE_PROVIDER=r2`。
3. 将公开媒体地址改为 R2地址。
4. 批量更新媒体表及文章HTML中的绝对媒体链接。
5. 重新构建前端，因为 Next.js图片域名白名单在构建期生效。
6. 重新部署后端，测试签名、上传确认、列表、读取和删除。

### 4. 验收

- 旧文章图片全部从 R2正常加载。
- 封面、头像、动态图片、音频和视频均可访问。
- 新文件只上传到 R2。
- 后台删除媒体时会删除正确的 R2对象。
- 正式页面不再请求 Supabase媒体地址。
- 桌面和手机页面均没有损坏的媒体内容。

### 5. 清理

1. 切换后至少保留 Supabase文件14天。
2. 验证完成后再次备份 TiDB。
3. 回滚期内停止 Supabase写入，但保留读取。
4. 回滚期结束并完成最终清单对比后，才能删除 Supabase文件。
5. 删除 Vercel中不再使用的 Supabase秘密。

## 九、旧博客迁移计划

- 读取 `koialkaid/blog` 中的 Markdown和 Frontmatter。
- 旧“文章”迁移为 pyq文章。
- 短“散记”迁移为动态，长“散记”迁移为文章，最终按内容复核。
- 将 Markdown转换为 pyq富文本编辑器兼容的安全HTML。
- 保留标题、摘要、发布日期、分类、系列、可表达的标签和原文地址。
- 使用与提供商无关的对象路径上传媒体。
- 保留旧 GitHub Pages作为归档。
- 在 pyq友链中添加 `https://koialkaid.github.io/blog/`。
- 生成导入成功、跳过和失败内容的迁移报告。

## 十、换电脑后的恢复方式

正式博客不依赖当前电脑。在新电脑上：

1. 克隆 `https://github.com/koialkaid/pyq`。
2. 安装 Node.js 20+和项目依赖。
3. 从密码管理器及各服务控制台取得秘密。
4. 仅在本地维护时重新创建 `backend/.env`。
5. 正常写作和管理通过线上 `/admin` 完成。
6. 升级前先查看当前版本文档及迁移要求，不盲目执行旧迁移脚本。

生产内容不在 GitHub中。完整恢复至少需要：

- GitHub仓库中的应用代码。
- TiDB导出文件中的文章、账号、评论和设置。
- 对象存储备份中的图片和其他媒体。

## 十一、以后绑定自有域名

首次部署使用平台免费子域名。购买域名后建议使用：

- `example.com`：前端。
- `api.example.com`：后端。
- `media.example.com`：R2媒体。

更换前端或媒体地址时必须同步更新 CORS和 Vercel环境变量。更换媒体域名后还必须重新构建前端。

## 十二、2026-08-10（新电脑接管与章节目录竞态修复）

- 完成新电脑接管文档、部署文档、后端部署说明、仓库约束和前端代理规则的阅读。
- 本地仓库位于 `D:\Koi-Blog\pyq`，接管时工作区干净，当前提交为 `3aa4be6`；尝试 `git pull --ff-only origin master` 时 GitHub 连接被重置，未覆盖任何本地文件。
- 使用本地便携 Git/Node.js 完成依赖安装和构建；未读取或输出 `backend/.env`、`backend/backups/` 内容。
- 修复文章详情 `ArticleTOC` 的章节/合集异步竞态：增加章节提取完成状态，合集请求不再依赖章节数量变化，只有确认无章节时才自动切换合集，并保留用户主动选择的目录模式。
- 后端 `npm.cmd run build` 和前端 `npm.cmd run build` 均通过；线上博客、文章目录、后台登录页和后端健康接口均返回 HTTP 200，健康接口状态为 `ok`。

### 2026-08-10（篇序显示与目录加载稳定性）

- 确认公开合集数据中存在 `seriesOrder=0` 的历史文章；前端不再直接把存储篇序作为显示编号，合集列表和文章详情侧栏统一按排序后的连续索引显示 `1、2、3...`。
- 文章详情目录改为等待正文 DOM 内容稳定后再完成章节提取，避免正文尚未渲染完成时被误判为“无章节”并自动切换到合集。
- 继续保持：存在章节时默认章节；只有稳定确认没有章节且存在合集时才自动显示合集；用户主动选择的目录模式不被异步请求覆盖。
- 前端生产构建通过。
- 对于已归属合集的文章，桌面侧栏从首次渲染起固定显示“章节 / 合集”分段结构；合集列表仍在加载时只显示加载占位，不再先出现旧版单独章节卡片再改变布局。

## 十三、2026-08-11（动态雨景与卡片透明度）

- 安装官方 `greensock/gsap-skills` 的 8 个技能，并在前端加入 `gsap` / `@gsap/react` 依赖。
- 新增公共页面全局雨景：原创程序化黑白湿润背景、斜向雨丝、雨滴落点水花和椭圆扩散波纹，不使用第三方图片或小予站点素材。
- 雨景 Canvas 使用 `requestAnimationFrame`、限制设备像素倍率和粒子数量；页面隐藏时暂停，移动端降低粒子密度，`prefers-reduced-motion` 下只保留静态背景。
- 所有公开博客主卡片和侧栏卡片默认使用 70% 不透明度，并提供前端显示设置，可在 45%–100% 间调整、恢复 70%、单独关闭动态雨景；偏好仅保存在当前浏览器本地，不写入生产数据库。
- 管理后台不加载雨景和透明度控制，避免影响后台操作界面。
- 前端生产构建、目标文件 ESLint、桌面 1440×900 和移动端 390×844 浏览器验收通过；无横向溢出，Canvas 尺寸和透明度实时调整符合预期。

### 2026-08-11（写实水面雨景与二级表面透明度）

- 将简单程序化渐变底图替换为真实低机位雨中水面摄影，并转为低饱和黑白 WebP；素材采用 CC0 公共领域许可，来源、作者和处理方式记录在 `frontend/public/images/RAIN-BACKGROUND-LICENSE.txt`。
- Canvas 继续叠加动态斜雨、水花和涟漪，图片加载失败时自动退回原程序化背景；深色模式增加独立遮罩，保持正文对比度。
- 卡片透明度滑块现同时控制文章预览/链接卡、留言输入框、顶部栏、底部操作栏及白色/气泡色内嵌按钮表面；只调整背景 alpha，文字、图标、封面和头像保持清晰。
- 二级表面使用主透明度的 55% 作为叠加 alpha，避免嵌套白底合成后反而比外层卡片更不透明。

### 2026-08-11（动态水体与流动折射）

- 写实水面不再只是静态底图：新增整片水体的低速横向漂移，以及按景深变化的分层折射位移，形成积水、池塘或近岸水面持续流动的观感。
- 水面远处位移轻微、近处波动更明显，现有雨丝、落点水花和扩散涟漪继续叠加在流动水体之上。
- 水面效果继续复用单个 Canvas 和同一动画循环，不创建额外 DOM 动画；移动端使用更宽的折射分层以降低每帧绘制次数。
- 关闭动态雨景、页面进入后台或系统启用减少动态效果时，水体动画与降雨动画一并暂停。

### 2026-08-11（透明雨景可读性增强）

- 针对透明卡片叠加动态水面后的低对比度问题，提高雨景模式中时间、来源、标签、友链说明、未选中筛选项、占位符和页脚等次要文字的明暗对比。
- 浅色模式次要文字统一调整为更深的中性灰，深色模式则使用更亮的中性灰；主文字、昵称色和链接色保持原有层级。
- 单独覆盖原先再次降低透明度的 `text-wechat-time/70` 日期样式，以及固定浅灰标签，避免小字号内容在 45% 卡片透明度下消失。
- 卡片透明度和动态水面保持不变，可读性修复只改变文字颜色，不通过提高整张卡片不透明度掩盖问题。

### 2026-08-11（雨声白噪音与站点级导航规划）

- 已备份 TiDB：15 张表、61 行；本次备份文件位于 `backend/backups`，按仓库规则忽略。
- 独立页面/工具统一使用导航名称“其它”，与首页内部的动态、文章、合集等内容区分。
- 雨声播放器规则确定为：每次进入页面随机选择一个许可确认过的雨声；同一次打开期间只循环当前音源，不在播放中途自动换声；用户关闭后重新打开或主动切换时才重新选择。
- 音频接入前必须记录 Freesound 文件详情页、作者、具体许可、预览/下载地址和本地文件校验信息；未确认具体许可的音频不得进入生产资源。

### 2026-08-11（雨声播放器与“其它”独立页面）

- 完成公共页面顶部悬浮导航：保留顶部安全间距，首页与“其它”作为顶级入口；文章、动态和合集继续属于首页内容体系，管理后台不显示公共导航。
- 新增独立 `/other` 页面，作为热力图等后续工具的统一承载入口。
- 新增站点级雨声控制：首次打开时随机确定音源，同一页面会话只循环当前音源；播放过程中不会自动换声，用户可暂停、调节音量或主动切换。
- 采用已核验的 Wikimedia Commons 公共领域 `Rain (1).ogg`，本地资源和许可记录位于 `frontend/public/audio/`；未核验的 Freesound 截图音源暂不进入生产。
- `frontend` 目标文件 ESLint 通过，生产构建通过；全量 ESLint 仍存在本轮之前的仓库级规则错误，未在本轮扩大修改范围。
- 已重新执行 `backend npm.cmd run db:backup`，生成 `tidb-2026-08-11T10-22-32-734Z.json`（15 张表、61 行）。接管环境当前没有 Git 可执行文件、GitHub 凭据或 Vercel CLI，因此本轮无法安全执行提交/推送/部署；代码和备份均已保留在工作区，待恢复发布工具链后继续。

### 2026-08-11（公共导航遮挡修复）

- 修复首页导航与主卡片顶部重叠：公共桌面卡片、左右侧栏和页面内顶栏统一下移到导航下方约 76px，并同步缩短可视滚动区域。
- `/other` 页面顶部间距从 112px 调整为 76px，约为原间距的四分之一量级；移动端顶栏也避开公共导航。
- 前端生产构建通过；目标文件 ESLint 仍仅报告仓库原有规则问题，未扩大修复范围。

### 2026-08-12（移除独立雨声播放器）

- 确认项目原有的 `R2 音乐歌单` 已提供后台上传、歌单管理和前端全局播放能力。
- 移除本轮新增的独立雨声播放器、静态 `rain-commons-1.ogg` 资源及其许可文件，避免与 R2 歌单形成两套音乐来源。
- 后续音乐统一从 R2 歌单读取；后台音频记录将补充名称、作者和音频源链接字段。
- WAV 是否可播放不由 R2 决定，而由文件内部编码和浏览器支持决定；当前上传规则已允许标准 `.wav`，建议优先使用 PCM WAV 或 MP3。

### 2026-08-12（歌单添加反馈与公共操作区布局）

- 修正歌单新增接口的排序值处理：统一将数据库返回的最大序号转为数字，避免空歌单或类型转换导致新增失败。
- 后台“添加到歌单”失败时同时显示页面提示和弹窗，直接呈现真实接口错误，避免按钮无响应的误解。
- 公共页面操作区改为常驻显示：透明度/雨景控制位于夜间模式按钮上方，夜间模式位于回顶按钮上方，调整了固定底部间距避免重叠。
- 已确认当前后端环境配置了 R2 存储变量；WAV 上传允许标准 `audio/wav` 与 `.wav` 扩展名，实际播放仍取决于 WAV 内部编码和浏览器兼容性。

### 2026-08-12（用户反馈问题记录）

- 夜间模式仍有少量黑色文字：初步定位为部分组件/文章嵌入内容使用硬编码 `#000`、`rgba(0,0,0,...)` 或固定浅色背景，没有完全使用站点主题变量；透明卡片叠加雨景后对比度问题会进一步放大。后续需要按组件逐项替换为主题变量并检查文章正文、代码块、链接卡片和合集目录。
- 进入合集后顶部排版混乱：页面内文章顶栏、公共站点导航、合集/章节侧栏分别使用固定定位，顶部安全区没有统一协调，导致导航和目录/正文首屏互相覆盖或间距异常。后续需要统一页面顶部布局基准，并为合集阅读区预留导航安全间距。
- 歌单上传流程容易被误解：后台“上传音频到云端”只是把文件上传并登记到媒体库；“添加到歌单”是第二步，将当前管理员本人名下的媒体记录关联到网站歌单。截图中的错误“请选择本人上传的云端音频文件”说明所选媒体记录的 `uploaderId` 与当前登录管理员不一致，或上传确认尚未完成，后端因此拒绝关联；这不是 R2 不支持 WAV 的提示。
- R2 与歌单的关系：R2/存储层负责保存音频文件，`music_tracks` 歌单表负责保存播放顺序和名称/作者等元数据，两者不是同一条记录；必须先成功上传并确认媒体，再加入歌单。

### 2026-08-13（单管理员与透明度默认值确认）

- 确认项目登录入口只有博客后台管理员账号，R2 不是独立后台，也不需要用户另行登录；R2 凭据仅由后端环境变量使用。
- 歌单音频归属校验调整为：媒体必须由管理员账号上传，兼容迁移记录或旧 token 导致的管理员 ID 不一致；普通用户媒体仍会被拒绝。
- 卡片不透明度调整为 `10%–100%`，默认 `45%`，恢复按钮恢复 `45%`；已有浏览器本地值只有在范围内才会继续使用。

### 2026-08-13（文章主题、合集顶部与歌单上传根因验证）

### 2026-08-13（歌单问题复核与浮动按钮主题同步）

- 按要求停止对生产 R2 和 `music_tracks` 的写入诊断；此前只读核验已确认 WAV 媒体记录存在、唯一管理员归属正确，生产加入歌单接口可正常返回 201，问题不在 R2 或 WAV 格式。
- 后台音乐页继续保留上传状态机：上传期间清空旧音频选择、显示云端上传中/成功/失败反馈，未完成上传时阻止加入歌单，避免把本地文件选择误当成云端媒体记录。
- 浮动操作按钮改为常驻显示，移除已经失效的滚动隐藏监听；夜间/日间按钮改用 `next-themes` 的 `resolvedTheme` 判断实际生效主题，和透明度按钮一致地随主题更新图标与颜色。
- 本轮未执行任何 R2 上传、删除、歌单新增或删除操作。由于当前终端未提供可执行的 Node/Git 路径，构建与提交待恢复本地依赖后执行。

- 生产数据确认只有 1 个用户且角色为管理员；目标 WAV 已多次成功上传到云端媒体库，格式为 `audio/wav`、大小约 13.2 MB，上传者与唯一管理员一致，因此 R2、WAV 格式和账号归属都不是失败原因。
- 使用唯一管理员身份和最新 WAV 对生产歌单接口执行可回滚诊断：新增返回 HTTP 201，随后删除临时记录返回 HTTP 204，证明生产歌单接口当前可正常接受该文件；诊断记录已立即清理。
- 根因定位为后台交互未区分“文件已选择”和“云端上传确认完成”：大文件上传期间已经显示本地文件名，但没有明显进度/完成状态，用户会在 `draft.audio` 尚未建立时点击添加。上传流程现增加“正在上传”“云端上传成功”“上传失败”反馈，未完成时禁止添加。
- 夜间黑字的生产正文证据为编辑器保存的 `<span style="color: rgb(34, 34, 34)">`；夜间模式现统一覆盖普通正文元素的行内颜色，同时保留链接、代码和高亮自己的颜色。
- 文章详情主卡片、页面内顶栏及章节/合集目录统一采用 76px 公共导航安全区，并同步缩短可滚动高度，修复进入合集/文章后顶部覆盖和排版混乱。
### 2026-08-13（云端音频全链路实测）

- 使用用户指定的 13,198,496 字节 WAV 文件完成正式环境全链路测试：管理员认证 HTTP 200、申请直传地址 HTTP 200、对象存储 PUT HTTP 200、媒体确认 HTTP 201、加入歌单 HTTP 201。
- 公开音频地址支持分段读取，返回 HTTP 206、`audio/wav`，总大小与本地文件完全一致；曲目 `Forestia - Rain on Leaves`（thinkingfish）已保留在正式歌单中。
- 关键结论：生产接口返回的媒体 `storageType` 是 `supabase`。当前生产实际使用 Supabase Storage，而不是 Cloudflare R2；后台旧称“R2 音乐歌单”只是历史文案，不能作为实际存储提供商的证据。
- 浮动夜间模式与回顶按钮保持常驻；主题切换改用 `resolvedTheme`，按钮背景、文字色及太阳/月亮图标随实际日间/夜间主题同步。
- 前端 Next.js 生产构建与后端 TypeScript 构建均通过；数据库备份 `tidb-2026-08-13T03-49-23-568Z.json` 完成（15 张表、84 行，Git 忽略）。
