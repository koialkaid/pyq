# Koi Blog 当前交接文档

更新时间：2026-08-14（Asia/Shanghai）

## 1. 当前结论

- 主仓库：`D:\Koi-Blog\pyq`
- Git 分支：`master`
- 本地与远程同步：`6c1c0ed Revert "feat: add rain garden visual prototype"`
- 当前网站代码中已经没有“其它 -> 雨庭”入口、页面或组件。
- 当前也没有秋日自然造景、Quaternius 资产或 Live2D 实现。
- 本轮没有修改生产数据库。
- 本轮没有读取、打印或提交任何 `.env` 内容。

## 2. 正式地址与仓库

- 博客：https://koialkaid-blog.vercel.app
- 文章目录：https://koialkaid-blog.vercel.app/articles
- 后台登录：https://koialkaid-blog.vercel.app/admin/login
- API 健康检查：https://koialkaid-blog-api.vercel.app/api/health
- GitHub：https://github.com/koialkaid/pyq
- 旧博客：https://koialkaid.github.io/blog/

## 3. 项目结构

- `frontend/`：Next.js 16 + React 19 + TypeScript + Tailwind，前台、后台页面和主题交互。
- `backend/`：Vercel API、TiDB 数据访问、Supabase Storage 媒体操作。
- `deploy/`：部署辅助配置。
- `项目日志.md`：按日期记录功能、验证和生产操作。
- `部署文档.md`、`backend/VERCEL_DEPLOYMENT.md`：部署资料。
- `新电脑GPT启动指令.txt`、`新电脑接管指南.md`：新环境接管入口。

## 4. 当前已经存在的主要功能

- 首页文章、章节/合集结构和文章详情页。
- 后台文章、章节、合集、媒体和音乐管理。
- 公开音乐播放、歌单关联、歌曲名称/作者/来源链接记录。
- 音乐文件上传到当前配置的 Supabase Storage；项目历史讨论中曾误称 R2，继续开发时应以代码和环境配置为准。
- 歌曲删除会处理歌单关联、媒体记录和云端对象；被其它地方引用的对象不会误删。
- 音乐、透明度、夜间模式、回顶按钮位于右侧浮动控制区，按钮间距已统一。
- 搜索按钮点击后在输入框内部右侧展开，图标不变色。
- 卡片透明度支持 10% 到 100%，默认 45%。
- 夜间模式文字、链接、代码块、目录和音乐卡片的主题颜色已完成一轮修复。

## 5. 已明确取消或回退的方向

- “其它 -> 雨庭”：已从当前代码删除。
- 雨、水面、鱼、雨线、涟漪视觉原型：已删除。
- Quaternius Stylized Nature MegaKit：仅作为原型使用，当前不在项目中。
- 挂机 / Live2D：只记录为未来需求，尚未开发；不要直接开始实现，先重新讨论方案和视觉原型。

## 6. 用户偏好与工作规则

- 用户要求先理解需求，再实施；视觉页面先本地预览，确认后才提交/部署。
- 涉及删除、回退、云端媒体操作时，先备份并核对目标，不能盲目修改。
- 每次回复开头使用“预计需要xx时xx分钟完成。”。
- 修改文件使用 `apply_patch`；保留用户已有改动，不做无关重构。
- 不读取或暴露 `backend/.env`、密码、Token、Secret Key、连接串和数据库内容。
- 生产数据库变更前必须先备份。
- 新窗口接管时先阅读顶层 `START-HERE-新电脑接管.txt`，再阅读本文件和 `项目日志.md`，然后检查 `git status --short --branch`。

## 7. 备份与回退记录

- 秋日自然造景原型的完整备份：
  `D:\Koi-Blog\.codex-tools\backups\20260814-nature-prototype-before-rollback`
- 该目录包含原型代码、Quaternius 资产副本和 `working-tree.diff`，仅用于恢复参考。
- 历史提交：
  - `40bb02d`：加入“其它 -> 雨庭”视觉原型。
  - `6c1c0ed`：正式回退该功能，当前线上同步到此提交。

## 8. 尚未排期的事项

1. 挂机 / Live2D 独立页面：仅保留需求记录，先做可行性和视觉方向讨论。
2. 自动化回归测试和线上验收脚本。
3. 后台云端媒体盘点、孤儿文件清理工具。
4. 旧文章缺图修复和旧博客迁移整理。
5. 如果未来需要真正迁移 Cloudflare R2，再单独制定迁移方案；不要把当前 Supabase Storage 与 R2 混为一谈。

## 9. 新窗口第一步

```powershell
Set-Location D:\Koi-Blog\pyq
git status --short --branch
git log -5 --oneline --decorate
```

确认当前仍是 `6c1c0ed` 后，再根据用户的新需求工作。不要从备份目录自动恢复雨庭或自然造景。
